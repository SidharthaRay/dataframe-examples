package com.dsm.optional

object LinkedListTest {
  def main(args: Array[String]): Unit = {
//    var first = new Node(1, null)
//    first = first.addAtEnd(new Node(2, null))
//    first.printtList()

  }


}


/*class Node(value: Int, var next: Node) {
  def addAtEnd(node: Node): Node = {
    this.next = node
    this
  }

  def printtList(): Unit = {
    if(this != null) {
      print(this.value + " -> ")
    }
    if(this.next != null) {
      var temp = this.next
      while(temp != null) {
        print(temp.value + " -> ")
        temp = temp.next
      }

    } else {
      print("Empty list")
    }
  }

}*/
